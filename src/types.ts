export interface Project {
  title: string;
  description: string;
  link: string;
  image: string;
  technologies: string[];
}

export interface Experience {
  company: string;
  position: string;
  period: string;
  description: string[];
}